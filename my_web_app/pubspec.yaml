name: app
description: "A new Flutter project."
publish_to: 'none'  # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  app_links: ^6.3.2
  firebase_core: ^3.6.0
  firebase_auth: ^5.3.1 # Firebase authentication
  cloud_firestore: ^5.4.4  # Firestore database
  async: ^2.11.0
  boolean_selector: ^2.1.1
  clock: ^1.1.1
  collection: ^1.18.0
  matcher: ^0.12.16+1
  # 他のパッケージも同様に更新

  # 必要に応じて他のFirebaseパッケージも追加

  csv: ^5.1.1  # ここで csv パッケージのバージョンを確認
